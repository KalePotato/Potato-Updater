# Configuration examples

These files document public JSON formats used by Seed, Updater, and the administrator packager. They are examples only and are not read by the build or automatically deployed.

All URLs use the reserved `example.invalid` domain. Private endpoint defaults are injected separately at build time.

Do not place credentials, private storage URLs, production-only payload lists, or user configuration in this directory.
